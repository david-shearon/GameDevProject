/// @description Enemy constructor

// Default values
hp = 100; // enemy will have 100hp by default, this can be changed in child instances
atk = 10; // enemy will have 10atk by default
spd = 3; // enemy will move at a default speed of 3
gun = noone; // the gun the enemy is holding
range = 1000; // visual range of the enemy

// Appearance
// sprite_index = 0; // the current frame of the animation
// image_speed = 0.2; // time to cycle through the animation frames

// Movement
// x = random(room_width); // random x position, TODO make this work with the map bounds
// y = random(room_height); // random y position, TODO make this work with the map bounds
